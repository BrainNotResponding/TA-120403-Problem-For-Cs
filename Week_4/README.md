# Program Flowchart, Pseudo-code, and Memory Mapping Guide

คู่มืออธิบายการทำงานของระบบคอมพิวเตอร์ผ่านผังงาน (Flowchart) รหัสเทียม (Pseudo-code) การจำลองพื้นที่ในหน่วยความจำ (Memory Map) และการวิเคราะห์ข้อผิดพลาดพื้นฐานในการเขียนโปรแกรม

---

## 1. คำอธิบายและการใช้งานสัญลักษณ์ผังงาน (Flowchart Symbols)

ตารางสรุปสัญลักษณ์มาตรฐานที่ใช้ในการออกแบบกระบวนการทำงานของระบบคอมพิวเตอร์

| สัญลักษณ์ (Symbol) | ชื่อเรียก (Name) | การทำงาน / สิ่งที่ต้องระบุภายใน | ทำเพื่ออะไร (Purpose) |
| :--- | :--- | :--- | :--- |
| **วงรี (Oval)** | Start / Stop | จุดเริ่มต้นและจุดสิ้นสุดของโปรแกรม โดยระบุคำว่า "start" หรือ "stop" ไว้ภายใน | เพื่อแสดงจุดเริ่มต้นและจุดสิ้นสุดของกระบวนการทำงานทั้งหมด |
| **สี่เหลี่ยมผืนผ้า (Rectangle)** | Process | การประมวลผล เช่น การคำนวณสูตรคณิตศาสตร์ หรือการกำหนดค่าตัวแปร (เช่น area = width * length) | เพื่อสั่งให้ระบบดำเนินการคำนวณหรือเปลี่ยนแปลงค่าข้อมูลภายใน |
| **สี่เหลี่ยมข้าวหลามตัด (Diamond)** | Decision | การตัดสินใจทางตรรกะ โดยระบุเงื่อนไขเปรียบเทียบ เช่น "x > 10" เพื่อแยกเส้นทางเป็น True หรือ False | เพื่อสร้างทางเลือกในการตัดสินใจ และกำหนดเส้นทางการประมวลผลขั้นถัดไป |
| **สี่เหลี่ยมด้านขนาน (Parallelogram)** | Input / Output | การนำข้อมูลเข้าหรือแสดงผลข้อมูลทั่วไป โดยไม่ได้เจาะจงชนิดของอุปกรณ์ | เป็นคำสั่งในการรับหรือส่งออกข้อมูลในระดับทั่วไป |
| **สี่เหลี่ยมคางหมูเอียง (Trapezoid)** | Input from Keyboard | รอรับข้อมูลจากผู้ใช้งานผ่านคีย์บอร์ด โดยระบุชื่อตัวแปรที่ต้องการนำไปเก็บค่า (เช่น read width) | เพื่อให้โปรแกรมสามารถรับค่าที่ยืดหยุ่นจากผู้ใช้งานขณะรันโปรแกรมได้ |
| **บล็อกปลายแหลม (Display)** | Output to Monitor | การแสดงผลลัพธ์ออกทางหน้าจอภาพ โดยระบุชื่อตัวแปรหรือข้อความที่ต้องการแสดงผล (เช่น print area) | เพื่อสื่อสารข้อมูล ผลลัพธ์ หรือข้อความแจ้งเตือนให้ผู้ใช้งานได้รับทราบ |
| **ทรงกระบอก (Database)** | Database / Magnetic Disk | การอ่านหรือเขียนข้อมูลลงในระบบฐานข้อมูลหรือดิสก์จัดเก็บข้อมูล | เพื่อบันทึกข้อมูลไว้ใช้งานอย่างถาวร (ไม่สูญหายเมื่อปิดโปรแกรม) |
| **วงกลมเล็ก (Circle)** | Connector | จุดเชื่อมต่อย่อยภายในหน้ากระดาษเดียวกัน | เพื่อเชื่อมโยงเส้นทางการไหลของข้อมูลที่มีความซับซ้อนให้เป็นระเบียบ |

---
<img width="875" height="652" alt="image" src="https://github.com/user-attachments/assets/7c3fd9b9-97b3-4b0e-a567-b43751281c1d" />

---

## 5. Detailed Guide: Pseudocode vs Flowchart (คู่มือเปรียบเทียบและการทำงาน)

ในการเริ่มต้นออกแบบโปรแกรม การทำงานร่วมกันระหว่าง **รหัสเทียม (Pseudocode)** และ **ผังงาน (Flowchart)** คือขั้นตอนสำคัญที่ช่วยเปลี่ยนตรรกะความคิดให้เป็นระบบก่อนนำไปเขียนโค้ดจริง (Coding) ซึ่งมีความสัมพันธ์และการแปลงรูปแบบขั้นตอนการทำงาน (Step-by-step Mapping) ดังนี้:

### 🔄 การจับคู่ขั้นตอนการทำงาน (Step-by-step Mapping)

เราสามารถแสดงการจับคู่เปรียบเทียบตรรกะการทำงานเดียวกันระหว่าง รหัสเทียม (เขียนเป็นข้อความสั้น ๆ ภาษาอังกฤษหรือไทย) และ ผังงาน (วาดเป็นบล็อกสัญลักษณ์) ได้ดังรูปด้านล่างนี้:

<p align="center">
  <img src="https://raw.githubusercontent.com/YOUR_GITHUB_USERNAME/YOUR_REPOSITORY_NAME/main/programming_logic_tools.png" width="650" alt="Programming Logic Tools: Pseudocode vs Flowchart">
</p>

*หมายเหตุ: อย่าลืมเปลี่ยนลิงก์ src ด้านบนให้เป็นตำแหน่งไฟล์รูปภาพจริงในคลังโปรเจกต์ของคุณบน GitHub*

---

### 📥 เปรียบเทียบขั้นตอนทำงานของทั้งสองเครื่องมือ (The Logic Process)

เพื่ออธิบายให้เข้าใจง่ายที่สุด เราจะใช้ตัวอย่างโปรแกรมคำนวณบวกเลขสองจำนวน **(Sum = Num1 + Num2)** ในการแสดงขั้นตอนการทำงานคู่ขนานกัน:

#### 1. จุดเริ่มต้นและจุดสิ้นสุด (Start & End)
*   **Pseudocode:** ใช้คำว่า `START` หรือ `BEGIN` ที่บรรทัดแรก และ `END` หรือ `STOP` ที่บรรทัดสุดท้ายเพื่อให้ทราบขอบเขตการทำงานของอัลกอริทึม
*   **Flowchart:** ใช้สัญลักษณ์ **Terminator (วงรีขอบมน)** ด้านในเขียนว่า `START` หรือ `END` เชื่อมโยงกันด้วยเส้นทางการไหลของข้อมูล (Data Flow)

#### 2. การรับข้อมูลเข้า (Input)
*   **Pseudocode:** เขียนสั้น ๆ เช่น `INPUT: Num1` และ `INPUT: Num2` (หรือ `Read Num1, Num2` ร่วมกันในบรรทัดเดียว)
*   **Flowchart:** ใช้สัญลักษณ์ **Input / Output (สี่เหลี่ยมด้านขนาน)** หรือในระบบที่เจาะจงการป้อนค่าทางคีย์บอร์ดจะเลือกใช้ **Input from Keyboard (สี่เหลี่ยมคางหมูด้านไม่เท่า)** ด้านในระบุชื่อตัวแปรที่รับค่ามาเก็บไว้

#### 3. การคำนวณและประมวลผล (Process)
*   **Pseudocode:** ระบุขั้นตอนสูตรตรง ๆ เช่น `Sum = Num1 + Num2` หรือใช้คำนำหน้าว่า `CALCULATE Sum = Num1 + Num2`
*   **Flowchart:** ใช้สัญลักษณ์ **Process (สี่เหลี่ยมผืนผ้า)** โดยการประมวลผลคำนวณใด ๆ จะถูกใส่ไว้ในกรอบสี่เหลี่ยมนี้เพื่อดำเนินการหาค่าผลลัพธ์

#### 4. การแสดงผลลัพธ์ (Output)
*   **Pseudocode:** ใช้คำว่า `PRINT Sum` หรือ `DISPLAY Sum` เพื่อสั่งให้อัลกอริทึมพ่นค่าผลลัพธ์ที่คำนวณเสร็จแล้วออกมา
*   **Flowchart:** ใช้สัญลักษณ์ **Input / Output (สี่เหลี่ยมด้านขนาน)** หรือเลือกใช้สัญลักษณ์เจาะจงอย่าง **Output to Monitor (ห้าเหลี่ยมปลายแหลม)** เพื่อระบุว่าข้อมูลนั้นกำลังถูกพ่นออกหน้าจอภาพให้ผู้ใช้งานมองเห็น

---

### 💡 สรุปประโยชน์ของการใช้เครื่องมือทั้งคู่ร่วมกัน
1.  **Pseudocode** ช่วยให้ผู้พัฒนาเรียบเรียงความคิดออกมาเป็นข้อความทีละบรรทัดได้อย่างรวดเร็ว มีลักษณะคล้ายการจดโน้ตสั้น ๆ
2.  **Flowchart** ช่วยเปลี่ยนข้อความเหล่านั้นให้กลายเป็นภาพที่มองเห็นโครงสร้าง ทิศทางการทำงานของเงื่อนไข และความสัมพันธ์ทั้งหมดได้อย่างเข้าใจง่ายภายในพริบตาเดียว
